# Client-safe only — set in Vercel Project → Settings → Environment Variables
# Local dev: copy to .env.local and fill from repo root .env (use VITE_ prefix):
#   VITE_SUPABASE_URL=<same as SUPABASE_URL>
#   VITE_SUPABASE_ANON_KEY=<same as SUPABASE_ANON_KEY>
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
