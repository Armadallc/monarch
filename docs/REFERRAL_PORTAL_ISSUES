Issues:

1. Change the Green “login link sent / use a different email” notification on the /login auth form
2. Change the referral form background color to Coconut #E9EDF6, all the text to Ash #2B2828, use Ash #2B2828 50% opacity for lighter text and borders. For status updates, warnings, failures, success badges/alerts use: Fire Sky #de7a5a, Ash Green #BDCDAD, Riverstone #87A79D, Sunbaked Clay #B89C30. We can use those colors as baselines and adjust the tint or shade if we need to. 
3. Change the color, text and style of the green success toast notification following public inquiry submission to be cohesive with the website. [✓ Check your email-We sent a login link to seth@rowandrive.com-Click the link in the email to access the secure referral form-The link expires in 1 hour-“Use different email”]
4. After logging out of portal, i attempted to login again using seth@rowandrive.com to see if the submitted referral loaded properly. It took over an hour to receive a code to my email.
5. Once we fix the login code —> email issue, i want to see if we can adjust the settings so that the link is sent faster AND i want to know if the links can arrive to uers email from Monarch, not supabase. This might be a premium feature. Please advise
6. After logging back into see if my referral was loading properly in my portal, i confirm it successfully loaded. However there is a problem. 
    1. When i clicked on the referral in the portal dashboard, it opens but i cannot scroll down to review the referral. The referral is not scrollable, and when i try to scroll down on the referral it remains open but the background is scrolling behind the referral. 
    2. Clicking the mouse outside of the referral closes the referral
    3. Clicking the X on the referral does NOT close the referrall
7. The Referral Progress tracker/timeline within the referral currently has 3 stages: Submitted, Under Review, Outcome. We need to gameplan how to implement a pre-review, post-review
check. The 3 stages can remain how they are because they make the most sense. However, if there are any questions, issues, missing documents or information that was missed on the referral which prevents us from properly reviewing it, this should be indicated between the 1st and 2nd stages as a smaller dot with “Issue” displayed beneath it. The same should display for any other issues if they arise between stages 2 and 3. If no issues arise between 1 and 2 then the dot will simply display the same green as the submitted stage and will display when the 2nd stage “Review” begins. The same will be true betwen stages 2 and 3 if no issues arise the dot will turn green at the time which stage 3 “Outcome” displays gree as complete. 
This involves simple, direct communication within the referral forms themselves which we already have. Lets see what our current communication capabililties are within the referral forms and what we need in order to communicate specific issues, have those issues alert either the referring party or the admissions team, and the alert being cleared in the same way as a task would be so that onces its done its cleared for both the referring source and admissions team; e.g. referral submitted —> admissions team sees the referral —> needs confirmation of restoration status + ID docs uploaded before we can begin review —> admissions team opens referral and creates a task. They must define the task and who the task is for by marking something like “internal” for staff or “source” to direct it back to the person who submitted the referral, then indicate (when it applies) if its an “issue” which means whatever the problem, its preventing us from going forward in either reviewing or completing our review. Think of “issue” as Urgent or High-Priority. Admissions marks the task with the priority level, marks what we need in order to proceed —> admissions sends referral back —> referring source sees the issue —> returns docs or whatever it is we need —> we receive —> proceed with review or complete review —> issue/alert clears —> referral process continues to its conclusion
8. Portal dashboard should mirror the ReferralDashboard table except without the internal functions of our admissions team. The referring source portal should display their submitted referrals with Checkbox, Client Name, Priority badge, Program (Which monarch program is handling the referral e.g. Competency, Mental Health etc. If the referral is transferred between one program and another it would update in this column, just as the previous column would update if the referral was reassigned), Court, Urgent Badge (we need to clarify the difference between high priority and urgent badges), Docs (attachments), Last Activity, Assigned (if/when the referral is assigned to someone on our admissions team, it would display in this column where the referring source can see who is handling the referral review), Status (read-only for referring source), Actions (View, X). The header should provide search and filtering capabilities. 
9. The width of the referring source portal should be as wide as necessary within the screen. Primarily for our desktop breakpoints, with tablet and mobile breakpoints that we can work on later. 

Referral Form:

Step 2: 
1. If “Emergency contact can provide collateral information” is marked YES, can we use this contact info to send a secure link to this referral so this contact can upload their collateral information e.e ROI, birth certificates, ID’s or other requested documents related to the referral? If so, then for any step within the referral form which asks if a contact can provide collateral information and its marked YES, then upon submitting the referral and a referral code is generated, it should be sent to those contacts requesting the specific documents or signatures from that contact. 
2. All emergency contacts, or third parties within the form which were marked YES to provide collateral information are listed as one of the last steps before completing the form. Each contact will have their names and fields to enter in their email, and a multi-select option of documents and/or actions which we are requesting from that contact. This list of names will be emailed with links upon submitting the form. This way we can view the list of all third parties which can provide collateral info in one place, add more names and emails if needed, and under each name should be a field for leaving a note or additional instructions. When a third party uses a link we will need to know when that link was used and who used it. If reference code is the same as the referral, we need a way to know which third party has responded and uploaded docs to that referral. So if Jim submits a referral for Chris (who Jim referred to us), and Jim sent out requests for a copy of Chris’s birth certificate to Jane (Chris’s mom) and Chris’s ID from Angie (Chris’s girlfriend) we will need a machanism in place to update the referral once Jane uploads Chris’s birth certificate, and when Angie uploads Chris’s ID. It can be as simple as changing the border of the referral form rows using semantic colors e.g. Unread referral = Ash Green background + Ash text + Ash border. Read referral = Coconut background + Ash text + 50% Ash border. Updated referral = Coconut background + Ash text + Ash Green border. Or so it doesn’t become visually overwhelming we can use simple dots at the far left of each referral row for read, unread, alerts (requests within a referral, messages in a referral etc), newly assigned referrals (when a referral is transferred, assigned, re-assigned etc. We need a way to indicate when a referral has been accepted
3. “Stepping” through the form: After completing a step and clicking “Next” the it transitions user to the next step at the bottom of that page. How can we fix this so that the beginning of the next step in the form loads at a consistent place on the screen?
4. Step 13: 
    1. Change the Submit Referral button from green to match the website colors. 
    2. Change form background colors to Coconut (SAFETY & RISK ASSESSMENT section background doesnt match the others)