# Dependencies
node_modules/
**/dist/
*.tsbuildinfo

# MCP / Framer session temp (repo root)
.mcp-*
.framer-upload-*

# Environment and secrets (never commit)
.env
.env.local
.env.*.local

# OAuth / Google credentials (client secret)
Google/

# Cursor MCP config (contains Framer MCP URL + secret)
.cursor/mcp.json

# Logs and OS
*.log
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
