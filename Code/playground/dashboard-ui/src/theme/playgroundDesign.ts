/**
 * Playground @design alias — thin re-export of canonical Code/theme.
 * CSS: Code/theme/dashboard-theme.css (imported in main.tsx).
 */
export {
    BRAND_FALLBACK,
    COLORS,
    RADIUS,
    FONT,
    FONT_HEADING,
    SHADOWS,
    TRANSITION,
    FROSTED_GLASS,
    FROSTED_GLASS_STRONG,
    BUTTON_PRIMARY,
    BUTTON_SECONDARY,
    INPUT_BASE,
    LABEL,
    CARD_HOVER,
    CARD_HOVER_STYLE,
    formatDisplayDate,
    normalizeCalendarDateIso,
} from "../../../../theme"
